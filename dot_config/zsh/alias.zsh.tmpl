# vim: ft=zsh
alias clear='echo -ne "\033c"'
alias c='clear'
alias df='df --human-readable'
alias du='dust --limit-filesystem'
alias fcp='rsync --archive --human-readable --partial --progress --copy-links --keep-dirlinks --no-inc-recursive'

# better ls
alias l="eza --icons=auto"
alias ll='l -l'
alias la='l -a'
alias lt='l -l -s modified'
alias tree="eza --tree --icons=auto"
alias tre="tree -L 2"

alias v='nvim'
alias vim='nvim'
alias vimdiff='nvim -d'
alias sys='systemctl'
alias che=chezmoi
alias cz=chezmoi
alias czcd="cd $(chezmoi source-path)"

{{- if eq .chezmoi.os "darwin" }}
# Tailscale cli
[[ -x "/Applications/Tailscale.app/Contents/MacOS/Tailscale" ]] \
    && alias tailscale="/Applications/Tailscale.app/Contents/MacOS/Tailscale"
{{- end }}

# additional user aliases
[[ -r "${ZDOTDIR}/alias.local" ]] && source "${ZDOTDIR}/alias.local"
